{{#each ideas}}
<div class="card card-body mb-2">
    <h4>{{title}}</h4>
    <p>{{details}}</p>
    <a class="btn btn-dark btn-block" href="/ideas/edit/{{id}}">EDIT</a>
</div>
{{else}}
    <p>No ideas listed</p>
{{/each}}